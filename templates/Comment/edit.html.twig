{% extends 'base.html.twig' %}

{% block title %}Edit comment{% endblock %}

{% block body %}
    <h1>Edit Comment</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn">{{ button_label|default('Update') }}</button>
    {{ form_end(form) }}

    <a href="{{ path('program_index') }}">{{ 'back-to-list'|trans }}</a>

{% endblock %}
